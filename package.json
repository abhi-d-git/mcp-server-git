{
  "name": "mcp-server-git",
  "version": "1.0.0",
  "description": "GitHub MCP server — repos, branches, PRs, commits",
  "type": "module",
  "main": "dist/index.js",
  "scripts": {
    "cleaninstall": "rimraf node_modules dist && npm install",
    "cleanbuild": "rimraf dist && npm run build",
    "cleanstart": "rimraf dist && npm run build && npm run dev",
    "build": "tsc",
    "start": "node dist/index.js",
    "dev": "tsx watch src/index.ts",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@abhinav-dev/mcp-platform-kit": "file:../mcp-platform-kit",
    "dotenv": "^17.4.2",
    "express": "^4.18.2",
    "zod": "^3.22.4"
  },
  "devDependencies": {
    "@types/express": "^4.17.21",
    "@types/node": "^20.11.0",
    "rimraf": "6.1.3",
    "tsx": "^4.7.0",
    "typescript": "^5.3.3"
  }
}
